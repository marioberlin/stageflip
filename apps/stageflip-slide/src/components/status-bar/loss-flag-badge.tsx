// apps/stageflip-slide/src/components/status-bar/loss-flag-badge.tsx
// Status-bar badge for the T-248 loss-flag reporter — scaffold; behavior
// lands in the follow-up `feat:` commit.

'use client';

import type { ReactElement } from 'react';

export function LossFlagBadge(): ReactElement | null {
  return null;
}
