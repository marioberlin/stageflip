# services/cv-worker/requirements-dev.txt
# Test + lint deps. Not shipped in the production container.

-r requirements.txt

pytest==8.3.3
httpx==0.27.2          # FastAPI TestClient transport
ruff==0.6.9
