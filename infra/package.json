{
  "name": "@stageflip/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Infra shape tests — Dockerfile + deploy workflow validation (T-231).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check tests",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "vitest": "2.1.9"
  }
}
