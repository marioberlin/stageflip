{
  "name": "@stageflip/tests-load",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "T-269 — K6 load test scenarios + Node helpers (seed/cleanup). K6 scripts run via `k6 run`; helpers run via tsx.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "@vitest/coverage-v8": "2.1.9",
    "typescript": "5.6.3",
    "vitest": "2.1.9"
  }
}
