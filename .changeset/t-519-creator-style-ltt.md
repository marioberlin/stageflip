---
'@stageflip/pack-creator-style': patch
---

T-519 — Creator Style Pack: fills the **Linus Tech Tips Pro register**
placeholder landed in T-516 with a real substantive preset
(`linus-tech-tips-pro-register`). Cluster F `lowerThird` clip (T-183
primitive); pure white `#FFFFFF` bar + LTT Blue `#0080FF` 6 px left-edge
accent strip + black `#000000` headline + (primitive-default)
accent-colored attribution; Mixed-Case headline
(`'iPhone 17 Pro vs Galaxy S25 Ultra'`) + UPPERCASE wide-tracked host
attribution (`'LINUS SEBASTIAN'`) — splits the cluster-F typography
difference (Mixed-Case headline like Vox + UPPERCASE attribution like
MKBHD); 300 ms slide+fade entrance / 2.5-second mid-hold / 250 ms
slide+fade exit — broadcast-pace tech-reviewer tempo FASTER than
T-517 MKBHD-pro + T-518 Vox-deluxe's 450 / 350 ms cinematic pacing,
closer to T-512 NBA Pro's broadcast scoring-strip tempo; proprietary
`LTT Display` BYO family + `Inter 700` OFL fallback declared
(primitive's hard-coded `Plus Jakarta Sans` renders at v1 per
T-323 / T-325 / T-507 / T-517 / T-518 cross-cluster pattern).
White-canvas shared with T-518 Vox-deluxe but blue-strip + UPPERCASE
attribution + slide animation distinguish LTT-pro from Vox's
yellow-strip + Mixed-Case + fade — these are the LTT-pro visual
signatures within the cluster-F register family. Manifest's
`contributes.presets[2]` updated; pack version unchanged (0.1.0; T-520
closes the pack). Prestige-creator (T-520) still placeholder.
