---
'@stageflip/engine': minor
---

Add `cluster-d-compose` handler bundle: `compose_title_sequence` / `compose_segment_open` / `compose_end_credits` (T-354). Closes the missing 9th canonical compose bundle (Cluster D — titles).
