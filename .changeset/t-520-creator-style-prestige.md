---
'@stageflip/pack-creator-style': minor
---

T-520 — Creator Style Pack: **closes the pack contributions** by adding
the fourth + final preset, the **Prestige Creator** cinematic title-card
composition preset (`prestige-creator`). UNLIKE T-517 / T-518 / T-519
(creator-specific REGISTER lower-thirds keyed to MKBHD / Vox / LTT
brand identities), `prestige-creator` is a composition template for ANY
creator who wants a high-end editorial-magazine title-card register —
Vanity Fair / The Atlantic / Apple TV+ documentary opener canon.
**First cluster-F `titleSequence` consumer** (cross-cluster register
reuse — the cluster-D prestige-TV title compositor primitive T-321
rebound from cluster-F via `PRESET_ID_BINDINGS['prestige-creator']`
per Pattern C; no new clipKind, no structural extension to the document
or binding model, just a new cluster-F preset). Deep-black `#000000`
full-bleed canvas; centered full-frame Mixed Case headline at 60 px /
fontWeight 700; UPPERCASE wide-tracked tagline at 24 px / fontWeight 500
/ letterSpacing 0.08em; `'plate-and-credits'` style bundle for the
two-shot title-plate + credits-block register; bespoke GT Sectra
proprietary-byo + Cormorant Garamond OFL fallback (cluster-D
transitional-serif-revival canon shared with T-348 stranger-things-
benguiat + T-349 got-trajan-clockwork). Slowest pacing of all four
Creator Style presets (~2× MKBHD's tempo) — 800 ms fade-in
EASE_OUT_QUART → ~5 s mid-hold → 600 ms fade-out EASE_IN_QUART; total
~6.4 s per cycle. No atmospheric companion clips (clean editorial canvas
distinguishes the cluster-F prestige-creator register from cluster-D
prestige-TV registers structurally and visually). signOff.parityFixture:
pending-user-review. Manifest version bumped 0.1.0 → 0.2.0 (minor;
additive feature — prestige-creator preset closes the pack to GA).
Description + keywords (`prestige`, `cinematic`) updated to surface the
four-preset complete contribution set.
