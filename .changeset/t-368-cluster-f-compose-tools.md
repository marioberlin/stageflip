---
'@stageflip/engine': patch
---

T-368 — Add the `cluster-f-compose` engine handler bundle: 4 read-only composer tools (`compose_creator_caption` / `compose_subtitle` / `compose_lyric_video` / `compose_keyword_highlight`) that bind a semantic Cluster F (Captions / Lyrics) brief to a ratified preset id + the echoed transcript / lyrics / keywords payload. Tools declare `ToolContext` (no document reads, no patch sink); the caller mounts the chosen clip via a separate write-tier tool (`add_clip` / `add_element`).

Dispatches across the 6 ratified Cluster F presets (T-362..T-367; ratified at T-316b closer 2026-05-06): `compose_creator_caption` covers 5 caption-clipKind presets (`hormozi` → hormozi-montserrat-black, `mrbeast` → mrbeast-komika-axis, `tiktok` → tiktok-rounded-box, `ali-abdaal` → ali-abdaal-opacity-karaoke, `netflix` → netflix-invisible; default `tiktok`); `compose_subtitle` defaults `accessibility_mode: 'strict'` → netflix-invisible (only FCC-grade preset in the cluster) and `relaxed` → tiktok-rounded-box; `compose_lyric_video` (only tool returning `clipKind: lyrics`) routes sealed-style → karaoke-progressive-wipe; `compose_keyword_highlight` defaults `hormozi` → hormozi-montserrat-black with `mrbeast` → mrbeast-komika-axis as the green-on-monetary subvariant. Mixed-clipKind dispatch (5 caption + 1 lyrics) handled via per-tool routing tables — the output envelope's `clipKind` is always present so the downstream `clip-animation` mount surface knows which clip to bind. Second cluster-composer bundle after T-340 (`cluster-b-compose`); precedes T-331 (Cluster A news) + T-361 (Cluster D ceremonial). No clip / parity-cli / preset markdown / parity golden touched.
