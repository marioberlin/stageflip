---
'@stageflip/pack-news-pro': patch
---

T-507 — News Pro Pack: fills the **Sky News register** placeholder
landed in T-506 with a real substantive preset (`sky-news-pro-register`).
Cluster A `lowerThird` clip; Sky News Red `#E10600` left strip on black
bar; Mixed Case headline + role; BYO Sky Text family + Plus Jakarta
Sans OFL fallback matching the primitive's hard-coded inline
`fontFamily`. Manifest's `contributes.presets[0]` updated; pack archive
rebuilt and verified through `check-pack-integrity`. ITV (T-508) +
RAI (T-509) still placeholders.
