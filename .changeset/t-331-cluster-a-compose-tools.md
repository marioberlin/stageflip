---
'@stageflip/engine': patch
---

T-331 — Add the `cluster-a-compose` engine handler bundle: 4 read-only composer tools (`compose_breaking_news` / `compose_ongoing_update` / `compose_guest_intro` / `compose_documentary_title_card`) that resolve a Cluster A (News & Broadcast) brief plus a brand-register hint into a ratified preset id + clipKind + rationale across the 8 ratified Cluster A presets (T-323..T-330; ratified 2026-05-06). Tools declare `ToolContext` (no document reads, no patch sink); the caller mounts the chosen clip via a separate write-tier tool (`apply_preset` / `add_clip` / `add_element`).

Dispatch (sealed): `compose_breaking_news` accepts `severity ∈ {urgent, developing, standard}` × `network ∈ {cnn, fox, msnbc, bbc, ajazeera, aplus}`; only `severity='urgent'` dispatches happy-path (cnn → cnn-breaking, fox → fox-news-alert, msnbc → msnbc-big-board, bbc → bbc-reith-dark, ajazeera → al-jazeera-orange, aplus → apple-tv-lt). `developing` / `standard` return `ok: false, reason: 'unsupported_severity'` — Cluster A canon ("Red = urgent") forbids painting non-urgent material in breaking register. `compose_ongoing_update` accepts only `network ∈ {cnn, bbc, ajazeera}` (the three networks with an ongoing-coverage register) and returns the matching lowerThird preset; `fox` / `msnbc` / `aplus` are rejected at Zod parse. `compose_guest_intro` couples `theme ∈ {broadcast, streaming}` with `network`: broadcast networks (cnn / fox / msnbc all fall back to cnn-classic per the Cluster A SKILL fallback rule, bbc → bbc-reith-dark, ajazeera → al-jazeera-orange) and streaming networks (netflix → netflix-doc-lt, aplus → apple-tv-lt); cross-theme combinations return `ok: false, reason: 'register_unavailable'`. `compose_documentary_title_card` is sealed at exactly two networks (netflix → netflix-doc-lt, appletv → apple-tv-lt). Third cluster-composer bundle after T-340 (`cluster-b-compose`) and T-368 (`cluster-f-compose`); precedes T-361 (Cluster D ceremonial). No clip / parity-cli / preset markdown / parity golden / cluster SKILL touched.

Wires: `CANONICAL_BUNDLES` += cluster-a-compose entry (placed alphabetically before cluster-b-compose); `gen-tool-skills` + `app-agent` orchestrator both register the bundle so the auto-generated `tools/cluster-a-compose/SKILL.md` picks up the 4 tools and the orchestrator dispatches them. Orchestrator smoke test bumped from 19 → 20 bundles.
