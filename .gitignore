# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Test artifacts
coverage/
.vitest/
playwright-report/
test-results/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.vscode/
.idea/
*.iml

# Claude Code harness state
.claude/

# StageFlip-specific
# Reference code is local-only; never committed. This is deliberate — see docs/architecture.md § 14.
# Anchored to repo root so `skills/**/reference/` (the skill-tier dir for
# auto-generated + hand-authored reference skills) is NOT matched.
/reference/

# K6 load-test outputs (T-269) — JSON summaries written by `pnpm load:smoke` /
# `pnpm load:full`. Local artifacts only; runs are tracked in ops dashboards.
/tests/load/runs/

# Render artifacts (local dev)
.stageflip-cache/
*.stageflip-render/

# Python (services/cv-worker)
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/

# Secrets / credentials
*.pem
*.key
firebase-adminsdk-*.json
